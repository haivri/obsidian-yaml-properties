{
  "name": "yaml-properties",
  "version": "1.0.14",
  "description": "Edit Obsidian properties as readable, syntax-highlighted YAML frontmatter and collapse them when not needed.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production",
    "lint": "eslint 'src/**/*.js'",
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "test": "node scripts/sync-themes.cjs --check && npm run build && node --test tests/*.test.cjs"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "yaml",
    "frontmatter",
    "properties",
    "syntax-highlighting"
  ],
  "author": "Robert Fleming",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "eslint": "^9.39.4",
    "eslint-plugin-obsidianmd": "^0.4.0",
    "globals": "^17.6.0",
    "obsidian": "1.13.1",
    "js-yaml": "^4.3.2"
  },
  "dependencies": {
    "@codemirror/state": "^6.5.0",
    "@codemirror/view": "^6.38.6"
  }
}
